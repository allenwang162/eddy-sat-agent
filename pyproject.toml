[project]
name = "eddy-sat-agent"
version = "0.1.0"
description = "FastAPI-based SAT practice and tutoring app."
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.115.6",
  "python-dotenv==1.0.1",
  "uvicorn[standard]==0.34.0",
]

[tool.uv]
package = false
